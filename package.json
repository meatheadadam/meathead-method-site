{
  "name": "meathead-method",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "scripts": {
    "build": "echo 'Static site — no build step'"
  }
}
