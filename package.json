{
  "name": "meathead-method",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/postgres": "^0.5.0"
  },
  "scripts": {
    "build": "echo 'Static site — no build step'"
  }
}